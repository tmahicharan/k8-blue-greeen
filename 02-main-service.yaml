apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    purpose: blue-green
    project: roboshop
    color: green
  ports:
    - protocol: TCP
      port: 80 # service port
      targetPort: 80 # container port